export enum WError {
  QUOTA_FULL = 'quota_full',
}

export enum BetError {
  MAX_OUTCOMES = 'max_outcomes',
  MAX_BIDS = 'max_bids',
  EXPIRED = 'expired',
  NO_RAISE = 'no_raise',
  FOLDED = 'folded',
}
